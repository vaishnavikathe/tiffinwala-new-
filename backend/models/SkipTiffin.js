/*import mongoose from "mongoose";

const skipTiffinSchema = new mongoose.Schema({
  userId: {
    type: mongoose.Schema.Types.ObjectId,
    ref: "User",
    required: true,
  },
  subscriptionId: {
    type: mongoose.Schema.Types.ObjectId,
    ref: "Subscription",
    required: true,
  },
  skipDate: {
    type: Date,
    required: true,
  },
  reason: String,
}, { timestamps: true });*/

//export default mongoose.model("SkipTiffin", skipTiffinSchema);