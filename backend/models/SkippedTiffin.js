/*import mongoose from "mongoose";

const skippedTiffinSchema = new mongoose.Schema({
  userId: {
    type: mongoose.Schema.Types.ObjectId,
    ref: "User",
    required: true,
  },
  subscriptionId: {
    type: mongoose.Schema.Types.ObjectId,
    ref: "Subscription",
  },
  skipDate: {
    type: Date,
    required: true,
  },
});
export default mongoose.model("SkippedTiffin", skippedTiffinSchema);*/